/* eslint-disable prettier/prettier */
import { View, Text } from 'react-native'
import React from 'react'
import HelpSupport from '~/components/profile/Help'

export default function support() {
  return (
    <View style={{ flex:1 }}>
      <HelpSupport />
    </View>
  )
}